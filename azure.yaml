# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json
name: ai-call-agent
metadata:
  template: ai-call-agent@0.0.1-beta
services:
  agent:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
